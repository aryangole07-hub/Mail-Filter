# Secrets - never commit these
credentials.json
token.json
.env

# Local state and output - digest_store.json holds the full text of your
# mail and feedback.json holds what you reported. Both are personal data and
# must never leave this machine.
last_run.json
digest_store.json
feedback.json
alerts_state.json
*.backup
*.bak
viewer.out
backfill.log
*.tmp
digest.log
digest.log.1

# Python
.venv/
__pycache__/
*.pyc

# Build output. dist/setup.exe embeds credentials.json - never publish it.
dist/
build/
*.spec
